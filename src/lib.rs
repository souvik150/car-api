// lib.rs
mod controllers;
mod dto;
mod models;
mod db;
mod config;

// You can also define common structures or traits here if needed.

pub mod error {
    // Define custom error types here.
}

pub mod utils {
    // Define utility functions here if needed.
}